```
Usage: peridio deployments delete --deployment-name <DEPLOYMENT_NAME> --product-name <PRODUCT_NAME>

Options:
      --deployment-name <DEPLOYMENT_NAME>  The name of the deployment to delete
      --product-name <PRODUCT_NAME>        The name of the product the deployment belongs to
  -h, --help                               Print help

```
