```
Usage: peridio config profiles create [OPTIONS] --name <NAME> --organization-name <ORGANIZATION_NAME>

Options:
      --name <NAME>
          The profile's name
      --organization-name <ORGANIZATION_NAME>
          The profile's organization_name
      --api-key <API_KEY>
          The profile's api_key. Can be omitted to supply the value interactively
      --no-input
          Disable interactivity
  -h, --help
          Print help

```